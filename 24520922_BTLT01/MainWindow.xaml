<Window x:Class="ChatGPT_Playground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatGPT_Playground"
        mc:Ignorable="d"
        Title="Playground" Height="450" Width="800"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Left"
                                      VerticalAlignment="Center"
                                      Margin="10,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F2F2F2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GrayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E8E8E8"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Margin" Value="3,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                        CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DADADA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#CFCFCF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#F5F5F5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="500"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="Overview" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Documentation" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="API reference" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Example" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Playground" Style="{StaticResource MenuButtonStyle}" Foreground="#4FCF75"/>
            </StackPanel>

            <Menu Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Background="Transparent">
                <MenuItem Header="⚡️Upgrade" Background="Transparent" Foreground="#4FCF75" FontWeight="SemiBold"  HorizontalAlignment="Stretch" Margin="0,3">
                    <MenuItem Header="Budget" Foreground="Black"/>
                    <MenuItem Header="Models" Foreground="Black"/>
                    <MenuItem Header="Limits" Foreground="Black"/>
                </MenuItem>
                <MenuItem Header="❓Help" Background="Transparent" Foreground="#333" FontWeight="SemiBold" HorizontalAlignment="Stretch" Margin="0,3">
                    <MenuItem Header="How to..."/>
                    <MenuItem Header="What is..."/>
                </MenuItem>
                <MenuItem Header="👤Personal" Background="Transparent" Foreground="#333" FontWeight="SemiBold" HorizontalAlignment="Stretch" Margin="0,3">
                    <MenuItem Header="Your profile"/>
                    <MenuItem Header="Terms and policies"/>
                    <MenuItem Header="Log out"/>
                </MenuItem>
            </Menu>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Background="#F5F5F5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0"  Content="Get started" FontSize="17" FontWeight="Bold" VerticalContentAlignment="Center" Margin="10,0"/>
                    <Button Grid.Column="1" Content="❌" Background="Transparent" Foreground="Black" BorderBrush="Transparent" Margin="0,0,10,0"/>
                </Grid>
                <TextBlock TextWrapping="Wrap" Margin="15" FontSize="14" Foreground="#333" TextAlignment="Justify">
                    <Run Text="Enter an instruction or select a preset, and watch the API respond with a completion that attempts to match the context or pattern you provided." FontSize="9"/>
                    <LineBreak/>
                    <LineBreak/>
                    <Run Text="You can control which model completes your request by changing the model." FontSize="9"/>
                    <LineBreak/>
                    <LineBreak/>
                    <Run Text="KEEP IN MIND" FontWeight="ExtraLight"  Foreground="#4FCF75"/>
                    <LineBreak/>
                    <Run Text="➤ Use good judgment when sharing outputs, and attribute them to your name or company. Learn more." FontSize="10"/>
                    <LineBreak/>
                    <Run Text="📈 Requests submitted to our API will not be used to train or improve future models. Learn more." FontSize="10"/>
                    <LineBreak/>
                    <Run Text="📅 Our default models’ training data cuts off in 2024, so they may not have knowledge of current events." FontSize="10"/>
                </TextBlock>
            </StackPanel>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid Grid.Row ="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    
                    <Label Grid.Column="0" Content="Playground" FontWeight="Bold" FontSize="15" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" HorizontalAlignment="right" Orientation="Horizontal">
                        <Button Content="Save" Width="40" Height="25" Style="{StaticResource GrayButtonStyle}"/>
                        <Button Content="View code" Width="70" Height="25" Style="{StaticResource GrayButtonStyle}"/>
                        <Button Content="Share" Width="45" Height="25" Style="{StaticResource GrayButtonStyle}"/>
                        <Button Content="..." Width="30" Height="25" Style="{StaticResource GrayButtonStyle}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="145"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="80"/>
                        </Grid.RowDefinitions>
                        
                        <Grid Grid.Row="0">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Run Text="Chat box :D"/>
                            </TextBlock>
                        
                        </Grid>
                        <Border Grid.Row="1"
                                CornerRadius="15"
                                Background="#F5F5F5"
                                Padding="8"
                                Margin="10">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="More"  Width="40" Click="AttachButton_Click" Style="{StaticResource GrayButtonStyle}"/>
                                <Popup x:Name="AttachPopup"
                                       PlacementTarget="{Binding ElementName=AttachButton}"
                                       Placement="Top"
                                       AllowsTransparency="True"
                                       StaysOpen="False">
                                    <StackPanel>
                                        <Button Content="📁  File"  HorizontalContentAlignment="Left"/>
                                        <Button Content="🖼️  Image" HorizontalContentAlignment="Left"/>
                                        <Button Content="🎤  Audio"  HorizontalContentAlignment="Left"/>
                                    </StackPanel>
                                </Popup>
                                
                                
                                <TextBox x:Name="ChatInput"
                                         Background="Transparent"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         VerticalAlignment="Center"
                                         FontSize="14"
                                         Margin="5,0"
                                         Foreground="Black"
                                         Text="" Width="320"/>
                                <Button Content="Send" Style="{StaticResource GrayButtonStyle}" Width="42" Background="#4FCF75"/>
                            </StackPanel>
                        </Border>

                    </Grid>
                    
                    
                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled" >
                        <StackPanel Background="White">
                            <Label Content="Mode"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" x:Name="ModePanel">
                                <ToggleButton x:Name="Mode1" Content="Mode 1" Click="ModeButton_Click" IsChecked="True"/>
                                <ToggleButton x:Name="Mode2" Content="Mode 2" Click="ModeButton_Click"/>
                                <ToggleButton x:Name="Mode3" Content="Mode 3" Click="ModeButton_Click"/>
                            </StackPanel>
                            <Label Content="Model"/>
                            <ComboBox Margin="5,0">
                                <ComboBoxItem Content="GPT-5" IsSelected="True"/>
                                <ComboBoxItem Content="GPT-5-pro"/>
                                <ComboBoxItem Content="GPT-5-nano"/>
                                <ComboBoxItem Content="GPT-5-mini"/>
                                <ComboBoxItem Content="GPT-4o"/>
                                <ComboBoxItem Content="GPT-4o-mini"/>
                                <ComboBoxItem Content="GPT-3.5-turbo"/>
                            </ComboBox>

                            <Label Content="Temperature"/>
                            <Slider Margin="5,0"/>

                            <Label Content="Maximum length"/>
                            <Slider Margin="5,0"/>

                            <TextBlock TextAlignment="Left">
                                <Label Content="Stop sequences"/>
                                <LineBreak/>
                                <Run Text="  Enter sequence and press Tab" FontSize="9"  Foreground="Gray"/>
                            </TextBlock>
                            <TextBox Margin="5,0" Height="25"/>

                            <Label Content="Top P"/>
                            <Slider Margin="5,0"/>

                            <Label Content="Frequency penalty"/>
                            <Slider Margin="5,0"/>

                            <Label Content="Presence penalty"/>
                            <Slider Margin="5,0"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>




        </Grid>
    </Grid>


</Window>
